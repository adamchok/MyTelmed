package com.mytelmed.common.constant.referral;

public enum ReferralPriority {
  ROUTINE, // Regular referral, no urgency
  URGENT, // Urgent referral, needs attention within days
  EMERGENCY // Emergency referral, immediate attention required
}